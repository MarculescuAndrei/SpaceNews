import { createContext } from "react";

export const UserContext = createContext<any>({});
export const LoginedUserContext = createContext<any>({});
export const isAuthContext = createContext<any>({});
